Метод myWriter принимает в качетве аргумента строку и записывает в файл полученную строку если такой строки в файле нету

Метод isFileExists принимает в качестве аргумента массив строк и возврашает строку, и проверяет есть ли файл подходящий первым 3 элементам массива
если нет то удаляется последний символ каждого элемента и проверяется заново и так максимум 3 раза
и если не было совпадений возврашает первый элемент массива и после будет создан файл с названием идентичный этому элементу
Если первый будет записан Иванов будет создан файл Иванов все Ивановы мужского и женского пола будут записаны в этот файл
Если первый будет записан Иванова будет создан файл Иванова но для фамилии Иванов будет создан файл Иванов, у меня так и не получилось решить эту проблему


Класс MyClass.MyHumanDataBase Метод myDataParseLine
Метод myDataParseLine проверяет соотвествие количества элементов к требуемой.
Метод считывает данные только по порядку т.е ФИО Дата рождения номер телефона и гендер.
любое изменение в последовательности выбросит ошибку MyClass.MyInvalidInputFormat с соответсвующим сообщением

Класс MyClass.MyHumanDataBase Метод myDataParse
Метод myDataParse как и метод myDataParseLine проверяет соотвествие количества элементов к требуемой.
Методу без разницы в каком порядке введены данные, если какой-то элемент не подходит ни одному из условий или
методу не удастся заполнить какое то поле выбросит ошибку MyClass.MyInvalidInputFormat с соответсвующим сообщением

Класс MyClass.MyHumanDataBase Метод isAlpha
Метод isAlpha принимает строку которую нужно проверить
и строку с условием ввида:
[a-zA-Z] или a-zA-Z
[a-zA-Z::aDc"@] или a-zA-Z::aDc"@
[::aDc"@] или ::aDc"@ после :: указывается символы для проверки без дипазона
и возврашает булево значение

Класс MyClass.MyHumanDataBase Метод isNumeric
Метод isNumeric проверяет что получено только цифры
и возвращает булево значение если вторым аргументом передано в методод false выбросит ошибку если условия не верны

Класс MyClass.MyHumanDataBase Метод isDateValid
Метод isDateValid принимает 3 аргумента - строку, тип даты, булево значение
в разультате работы возврашает булево значение, если терьим аргументом передано в методод false выбросит ошибку если условия не верны